@model IEnumerable<nm916616MIS4200.Models.Orders>

@{
    ViewBag.Title = "Order Index";
}

<h2>Order Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer.fullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orderDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.customerFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orderDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.orderNumb }) |
            @Html.ActionLink("Details", "Details", new { id=item.orderNumb }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.orderNumb })
        </td>
    </tr>
}

</table>
